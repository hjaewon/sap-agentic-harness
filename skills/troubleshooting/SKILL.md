---
name: troubleshooting
description: Harness-neutral diagnostics and operations for the SAP MCP server — connection checklist, SAP system identification, RFC backend selection criteria, profile/tier checks, and blocklist verification.
---

# troubleshooting (wrapper)

1. Resolve project context first: read `${CLAUDE_PLUGIN_ROOT}/core/project-context.md` and the project's `.sc4sap/config.json`.
2. Read `${CLAUDE_PLUGIN_ROOT}/core/procedures/troubleshooting.md` and follow it exactly, in order, honoring every gate.
3. Policies in `${CLAUDE_PLUGIN_ROOT}/core/policies/` override convenience. Personas live in `${CLAUDE_PLUGIN_ROOT}/core/personas/`.

Task: {{ARGUMENTS}}
