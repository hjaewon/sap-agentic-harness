# 5-13 층2 — JNC 교훈 팩 층2(지식) ↔ conventions 이식 선결 판정 감사 (2026-07-18)

- **기준(원천)**: `D:\Claude for SAP\JNC-Dashboard\docs\reference\sap-adt-lessons-pack.md`
  — 층 2 절(105~136행, 8항목) + 부록(162~168행)
- **정본 후보(원본 신판)**: 동결 `sc4sap-custom` HEAD(`ffb422b`)의 `common/*.md` 5파일 —
  수정 3종(`clean-code.md`·`field-typing-rule.md`·`function-module-rule.md`) + 신규 2건
  (`abapgit-roundtrip-rule.md`·`source-repair-protocol.md`). 07-17 커밋 `f7257c0`+`ffb422b`로
  갱신됨(동결 표기 하 드리프트).
- **이식 목적지**: `interactive/core/knowledge/abap/conventions/` (현재 18파일 → 20파일)
- **판정 원칙**: 문구가 아니라 실물 대조. 동결 레포는 읽기 전용(git status/show/diff만) —
  본 감사는 코드/지식 무수정, 산출은 이 문서 1개.
- **형식 참고**: `docs/reference/audits/2026-07-18-5-13-layer1-audit.md`

---

## 0. 판정 요약 (정본 결론)

**정본 = 원본 신판(sc4sap HEAD `common/*.md` 5파일).** 팩 층2 8항목 전부가 신판 5파일에
반영돼 있고(누락 0), 신판은 팩을 **상회**한다 — 층1 팩 항목 #2/#5/#6(inactive-version 함정·
sibling false-failure·active≠activation)을 `source-repair-protocol.md`에, 부록의 조건부
2건(abapGit 서버별 재실측·SUSH 버전의존)을 `abapgit-roundtrip-rule.md`의 caveat 절로 이미
흡수했다. 따라서 이식 = **신판 5파일 기준 copy(본문 verbatim) + 링크경로 정합**, 팩만
있고 신판에 없는 보완 항목은 **없음**.

- **이식 방식**: L1 관례와 동일 — 본문 verbatim 복사, 링크 경로만 목적지 트리에 맞춰 정합.
  단 이번 5파일의 신규/추가 링크는 **전부 conventions/ 내부 형제**(`function-module-rule.md`·
  `abap-release-reference.md`·`clean-code.md`·`source-repair-protocol.md`·`abapgit-roundtrip-rule.md`)라
  **복사 시 링크 재경로 0건**. (L1의 clean-code.md에서 났던 `data-extraction-policy.md` 류
  외부 재경로가 이번 추가분엔 없음.)
- **하네스 중립성**: 추가된 5파일 내용에 sc4sap 고유 참조(엔진 버전·`acknowledge_risk`·모델
  라우팅·sc4sap 전용 에이전트/스킬명·`context-loading-protocol` 링크·JNC 경로·`.sc4sap/` 경로)
  **0건**. 적응 지시 불필요(그대로 이식).
- **파급**: 코어 지식 복사 외에 sc4sap가 같은 커밋에서 함께 배선한 **페르소나 2·절차 2·인덱스
  수치 2**를 하네스 대응 지점에 반영해야 층2 완결(§5 체크리스트).

---

## 1. L1 이식 관례 판정 (이번 이식의 기준)

본 레포 conventions의 드리프트-이전 판(= sc4sap `f7257c0^`, 즉 L1 이식 원천)과 현재 repo
파일을 대조(줄끝 CR 무시):

| 파일 | 대조 결과 | 관례 |
|---|---|---|
| `field-typing-rule.md` | **완전 동일(byte-identical)** | verbatim copy |
| `function-module-rule.md` | **완전 동일** | verbatim copy |
| `clean-code.md` | **2줄만 상이** — 둘 다 `data-extraction-policy.md` 링크 경로: sc4sap `common/` 형제 → repo `../../../policies/data-protection/`로 재경로 | verbatim + **재배치된 링크만 경로 정합** |

**확정 관례**: 이식은 **본문 문자열 verbatim 복사**이며, 유일한 적응은 **다른 디렉터리로
재배치된 링크 대상의 상대경로 정합**뿐이다. 주목 — repo의 `field-typing-rule.md`·
`function-module-rule.md`는 "made by sc4sap skills or agents", `.sc4sap/cbo/…`,
`skills/create-object/…`, `agents/sap-executor.md` 등 **sc4sap 고유 산문 참조를 그대로 보존**한
채 이식됐다(제거·일반화하지 않음). 즉 **산문 내 sc4sap 언급은 손대지 않고, 깨지는 링크
경로만 고친다**가 이 레포의 이식 계약이다. 이번 층2도 동일 계약을 적용한다.

> 시사점(§4): 신판 추가분에 sc4sap 고유 참조가 있더라도 L1 관례상 산문은 verbatim 유지가
> 원칙. 다만 이번엔 추가분에 그런 참조가 애초에 0건이라 판단 여지가 없다.

---

## 2. 8항목 커버리지 표 (팩 항목 ↔ 신판 좌표 ↔ 이식 목적지)

전부 신판 반영 완료(누락 0). "좌표"는 동결 HEAD 기준, "목적지"는 repo conventions/ 기준.

| # | 팩 층2 항목(요지) | 신판 좌표(sc4sap HEAD) | 이식 목적지(repo conventions/) | 판정 |
|---|---|---|---|---|
| 1 | 표준 테이블 필드명 = DD03L 실측 정본(R-004); VBAK~WAERK/AWTYP/COSS/PLPO 반례 | `field-typing-rule.md` § Lookup Protocol 신설 **step 0** | `field-typing-rule.md` 동일 위치 | 완전 반영 |
| 2 | 필드 의미 ≠ 필드명/도메인명(R-011); T001B-BUKRS/OPVAR·SKA1-VBUND/RCOMP·SHARED vs PARTITION 실측 | `field-typing-rule.md` § "Anti-Pattern — Field Name / Domain ≠ Meaning" (신설) | 동일 | 완전 반영 |
| 3 | RFC FM 인터페이스: 제네릭타입 거부·구체 DDIC 필수·`ABAP_BOOL`(도메인)→`ABAP_BOOLEAN`(DE) | `function-module-rule.md` § "RFC Interface Type Constraints" (신설) | 동일 | 완전 반영 |
| 4 | 좁은 DEC/CURR에 비율·백분율 산술 금지(R-013); COMPUTE_BCD_OVERFLOW·정적게이트 무력·RFC 덤프無·caller 소유·독립 프로브 | `function-module-rule.md` § "Narrow DEC Fields — BCD Overflow…" (신설) **+** `clean-code.md` § Variables and Types 불릿(교차참조) | 두 파일 동일 위치 | 완전 반영(2곳) |
| 5 | abapGit 왕복 규율(R-003+M4): LF/BOM·`.gitattributes eol=lf`+ZIP CRLF 0검증·FUGR delete-and-recreate 미러 완전성·overwrite-all 정상·SUSH 스킵·구조체 직렬화(ROLLNAME/COMPTYPE E, DATATYPE/LENG/DECIMALS, CURR REFTABLE/REFFIELD) | **신규** `abapgit-roundtrip-rule.md` 전체 | **신규** `abapgit-roundtrip-rule.md` | 완전 반영(+부록 흡수) |
| 6 | FUGR = 단일 컴파일/활성화 단위; 그룹 0/0 전제·형제 결함이 내 FM 활성화 차단 | `function-module-rule.md` § "Function Group Is One Compile and Activation Unit" (신설; UXX 불릿은 `ffb422b`) | 동일 | 완전 반영 |
| 7 | 수선 기법: 서버 소스 읽고(재편집이면 inactive) 최소편집→되읽기→즉시 미러 | **신규** `source-repair-protocol.md` 전체 | **신규** `source-repair-protocol.md` | 완전 반영(+층1 #2/#5/#6 흡수) |
| 8 | 값 부재를 0으로 읽지 마라(R-002); 빈 합계 0=0 위장·RFC TABLES null 불가(→0.00)·caller 정규화 훅·빈 rows면 합계 미탑재 | `clean-code.md` § "Reconciliation Logic — Null vs Zero" (신설) | 동일 | 완전 반영 |

**결론**: 팩만 있고 신판에 누락된 항목 = 0. 보완 방안 불요.

---

## 3. 파일별 이식 지시 (이 문서만으로 실행 가능)

원천 = 동결 `sc4sap-custom` HEAD `common/<파일>`. 각 추가 블록은 **verbatim**. 링크는 전부
conventions/ 내부 형제라 **재경로 불필요**. 3종 수정은 기존 repo 파일에 **추가만**(기존 본문
불변), 2건 신규는 **전문 복사**.

### 3-A. `clean-code.md` (수정 — 2곳 추가)

1. **§ Variables and Types**의 "Never reuse a variable…" 불릿(repo 28행) **바로 다음**에 추가:
   ```
   - **Ratio / percentage arithmetic assigned into a narrow DEC/CURR field is a runtime-only defect class** (`COMPUTE_BCD_OVERFLOW` fires only on values, never at syntax check) — see [`function-module-rule.md`](function-module-rule.md) § Narrow DEC Fields — BCD Overflow.
   ```
2. **§ Exception / Error Handling** 블록(repo 80~87행) **다음**, `## Open SQL`(repo 89행)
   **앞**에 신설 절 삽입:
   ```
   ## Reconciliation Logic — Null vs Zero

   Never read an absent value as `0` in verification / reconciliation logic — a failed or empty lookup then masquerades as "difference = 0, match ✓" (two empty totals compare `0 = 0` and the check passes on nothing). If the underlying rows are empty, do not emit totals at all — block the zero assertion rather than let it read as a match. RFC `TABLES` parameters cannot carry null (an uncomputed value arrives as `0.00` — see [`function-module-rule.md`](function-module-rule.md)), so design a caller-side normalization hook that maps "no rows / not computed" to null before any comparison.
   ```
   - 링크 `function-module-rule.md` = 형제, 그대로.

### 3-B. `field-typing-rule.md` (수정 — 2곳 추가)

1. **§ Lookup Protocol**에서 "1. Standard DE search"(repo 50행) **앞**에 step 0 삽입:
   ```
   0. **Verify standard-table field names against the live system FIRST (standard tables only)** — before enumerating or typing any field of a *standard* SAP table, confirm the actual field names via `GetTable` or a single-column `GetSqlQuery` (`SELECT fieldname FROM dd03l WHERE tabname = '<TAB>'` — single-column deliberately, so the multi-column result-shift risk never applies). Never write standard-table field names from memory or from a design document — an offline ABAP parser cannot catch a wrong field name in principle (the name is syntactically valid, only semantically absent). Counterexamples that were all wrong when trusted from memory: `VBAK-WAERS` → the real field is `WAERK`; `AWTYPE` → real `AWTYP`; `COSS-WKG_WRT` does not exist at all (the period amounts live in columns `WKG001`–`WKG016`); `PLPO-ARBPL` → the work center is not on `PLPO`, it lives on `CRHD` (join `PLPO-ARBID` → `CRHD-OBJID`). New CBO fields are defined by you and skip this step.
   ```
2. **§ Common Standard DE Reference** 절 끝(repo 79행 "Document negative searches…" 다음),
   `## Integration Points`(repo 94행) **앞**에 신설 절 삽입:
   ```
   ## Anti-Pattern — Field Name / Domain ≠ Meaning

   Never infer a field's business meaning from its name or its domain name alone — SAP reuses both, and the name/domain will steer you into the wrong join or the wrong data model.

   - `T001B-BUKRS` carries domain `OPVAR` — it is the **posting period variant**, NOT the company code, despite the `BUKRS` field name.
   - Conversely `SKA1-VBUND` carries domain `RCOMP` yet **IS** the trading partner, not a generic company-code field.

   Confirm meaning via the data element's semantics (`GetDataElement` → label + documentation), never the field name or the domain name. For shared-vs-org-specific data-model decisions (one shared table vs an org-partitioned one), do not decide from the key structure — **measure the actual data**: values that cross org units → the entity is SHARED; values dedicated to a single org unit → PARTITIONED.
   ```
   - 링크 없음(툴명 `GetDataElement` 등은 하네스 중립).

### 3-C. `function-module-rule.md` (수정 — 1곳 확장 + 3절 신설)

1. **§ Key constraints #4**(repo 62행) 문장 끝에 이어붙임:
   ```
    RFC `TABLES` parameters cannot carry null — an uncomputed value arrives as `0.00`, never as "absent". Normalize absence to null on the caller side; see [`clean-code.md`](clean-code.md) § Reconciliation Logic — Null vs Zero.
   ```
2. 파일 **끝**(repo 115행 "Do not attempt to set the flag…" 다음)에 3절 추가:
   ```
   ## RFC Interface Type Constraints

   RFC-enabled FM parameters reject generic types — `TYPE P DECIMALS 2` (and any other non-concrete type) is rejected; every RFC parameter needs a concrete DDIC type (a data element, or a DDIC structure / table type). `ABAP_BOOL` is a **domain**, not a data element — never place it in a data-element slot; use `ABAP_BOOLEAN` (the data element built on that domain). See [`abap-release-reference.md`](abap-release-reference.md) for the `ABAP_BOOL` → `ABAP_BOOLEAN` release note.

   ## Narrow DEC Fields — BCD Overflow Kills the Whole Call

   Never assign percentage / ratio arithmetic into a narrow DEC field (e.g., `DEC(5,2)`). The moment real data pushes the ratio past ±999.99, `COMPUTE_BCD_OVERFLOW` aborts the **entire FM call** at runtime — not just the offending statement. No static gate catches this: syntax check, code review, and activation all pass, because the defect fires only on values, never on the source. Worse, when the FM is called over RFC the runtime error can return to the caller without leaving an ST22 dump on the server — there is nothing to find after the fact.

   Rule: ratio / percentage math belongs to the caller (e.g., Java `BigDecimal`), not to a narrow ABAP DEC/CURR result field. If an RFC call fails with no explanation, capture the exception verbatim with an independent standalone probe — a minimal separate caller that does nothing but invoke the FM and surface the exception, not the full application — so the raw runtime text is preserved.

   ## Function Group Is One Compile and Activation Unit

   The whole function group compiles together — group syntax must be **0 errors / 0 warnings** before activation, and a defect in a **sibling FM** blocks activation of *your* FM too (you cannot activate one FM of a defective group in isolation).

   - **Diagnosis leverage**: run the server-side check per FM — `CheckSyntax` with `object_type='function_module'` plus the shared `function_group_name` returns that module's errors with `line:column` (read-only). Because the group compiles as one unit, sibling defects surface in these checks too. Do NOT try to shortcut via `object_type='program'` on the `SAPL<fugr>` main program — that path checks only the main program body and misses errors inside the FM includes.
   - **False-failure trap**: FM write tools postcheck the whole group, so a pre-existing sibling defect can make your own write look *failed* while the write itself persisted. Re-read the FM to verify before re-writing — see [`source-repair-protocol.md`](source-repair-protocol.md).
   - **Never put the system UXX include (`L<fugr>UXX`) in an activation list.** It is a generated container editor-locked by `SAP*`. When building an `ActivateObjects` list by mechanically enumerating group members (includes list, object structure), filter `L<fugr>UXX` out — on the legacy `/sap/bc/adt/activation` endpoint one UXX entry kills the ENTIRE run with 403 `ExceptionResourceNoAccess` ("Changes to L...UXX are forbidden by SAP*"; live-verified on S/4 2021, 2026-07-17). The proven FUGR family is exactly: `SAPL<fugr>` main program + `L<fugr>TOP` + every FM + the FUGR itself — UXX is not part of it. (Behavior of the `/sap/bc/adt/activation/runs` mass endpoint with a UXX entry is unmeasured — exclude it there too.)
   ```
   - 링크 `abap-release-reference.md`·`clean-code.md`·`source-repair-protocol.md` = 전부 형제, 그대로.

### 3-D. `abapgit-roundtrip-rule.md` (신규 — 전문 복사)

동결 `common/abapgit-roundtrip-rule.md` HEAD 전문을 `conventions/abapgit-roundtrip-rule.md`로
그대로 복사. 외부 링크 0, sc4sap 참조 0. 절 구성: LF only / FUGR Pull Is Delete-and-Recreate /
Overwrite-All on Pull Is Normal / Skip SUSH Delete Proposals / Structure Serialization Fields /
Caveat — Re-Verify Per Server. **부록의 abapGit·SUSH 조건부가 이 파일의 caveat로 이미 내장**됨.

### 3-E. `source-repair-protocol.md` (신규 — 전문 복사)

동결 `common/source-repair-protocol.md` HEAD 전문을 `conventions/source-repair-protocol.md`로
그대로 복사. 절 구성: Read Before Edit / Inactive-Version Trap / "Active Source Returned" Is Not
Activation Evidence / Sibling-Defect False Failure / Identity-Write Isolation. 링크
`function-module-rule.md`·`abapgit-roundtrip-rule.md` = 형제, 그대로. sc4sap 참조 0.

---

## 4. 신판 5파일 하네스 중립성 점검 (적응 지시)

점검 대상 토큰: 엔진 버전 · `acknowledge_risk` · 모델 라우팅 · sc4sap 전용 에이전트/스킬명 ·
`context-loading-protocol` 링크 · JNC 프로젝트 경로 · `.sc4sap/` 경로.

| 파일(추가분) | sc4sap 고유 참조 | 적응 지시 |
|---|---|---|
| `clean-code.md` 추가 2곳 | 없음 (링크는 형제 `function-module-rule.md`) | 그대로 |
| `field-typing-rule.md` 추가 2곳 | 없음 (툴명 `GetTable`/`GetSqlQuery`/`GetDataElement`는 하네스 공통) | 그대로 |
| `function-module-rule.md` 추가 4블록 | 없음 (링크 3개 전부 형제; "Java BigDecimal"은 일반 예시) | 그대로 |
| `abapgit-roundtrip-rule.md` (신규) | 없음 (`core.autocrlf`/`.gitattributes`/abapGit/SUSH = 일반 기술 용어) | 그대로 |
| `source-repair-protocol.md` (신규) | 없음 (`Update*`/`GetInactiveObjects`/`version=inactive` = ADT 공통) | 그대로 |

**판정**: 추가분·신규분에 **적응 필요 지점 0**. L1 관례(verbatim + 링크경로 정합)와 일관 —
이번엔 재경로 대상 링크마저 없어 순수 verbatim. `context-loading-protocol.md` 링크를 참조하는
추가분 없음(그 파일은 매니페스트 obsolete). 기존 repo 파일 본문에 이미 남아있는 sc4sap 산문
참조(`.sc4sap/cbo/`, "sc4sap skills or agents" 등)는 L1 verbatim 이월분으로 **본 층2 범위 밖**
— 손대지 않는다.

---

## 5. 파급 효과 체크리스트 (실측 확정)

코어 지식 복사(§3)에 더해, sc4sap가 **같은 커밋 `f7257c0`에서 함께 배선**한 지점의 하네스
대응물. sc4sap 커밋 메시지: "wire Tier-3 triggers, executor/debugger convention tables, phase4
FUGR activation recipe, phase6 checklist items, CLAUDE.md index rows". "모든 프로젝트 세션이 자동
수혜"(팩 층2 정의)를 위해선 이 배선까지가 층2의 일부다.

### 5-1. 페르소나 (직접 파리티 — 권고 적용)

| repo 파일 | 앵커 | 반영 내용 | sc4sap 원본 |
|---|---|---|---|
| `interactive/core/personas/sap-executor.md` | `<Shared_Conventions>` 표(56~74행) | 표 하단에 **2행 추가**: abapGit round-trip / source repair. 경로 접두 `../knowledge/abap/conventions/` (sc4sap `../common/` → 하네스 정합 = L1 링크관례) | `agents/sap-executor.md` +2행 |
| `interactive/core/personas/sap-debugger.md` | `<Constraints>` "Fix with minimal diff…"(39행) 다음 | **1줄 추가**: 기존 객체 패치 전 `source-repair-protocol.md` 따르기(inactive 읽기·최소편집·되읽기). 경로 `../knowledge/abap/conventions/source-repair-protocol.md` | `agents/sap-debugger.md` +1줄 |

- 배선 표 2행(executor) 예시:
  `| abapGit round-trip discipline (LF/BOM, FUGR mirror completeness, pull = delete-and-recreate, SUSH skip) | [`../knowledge/abap/conventions/abapgit-roundtrip-rule.md`](…) | abapGit ZIP export/import or bulk multi-FM repair |`
  `| Source repair protocol (read-before-edit, inactive-version trap, activation evidence, sibling-defect false failure) | [`../knowledge/abap/conventions/source-repair-protocol.md`](…) | Any `Update*` on an object not created this session |`

### 5-2. 절차 (직접 파리티 — 권고 적용)

| repo 파일 | 앵커 | 반영 내용 | sc4sap 원본 |
|---|---|---|---|
| `interactive/core/procedures/create-program.md` | Phase 4(380행)의 활성화 단계 | **FUGR 활성화 레시피 블록** 추가(SAPL+TOP+전 FM+FUGR 단일 run·무관 객체 혼입 금지·형제 결함 체크). 링크 `../knowledge/abap/conventions/function-module-rule.md` § Function Group… | `skills/create-program/phase4-parallel.md` +1블록 |
| `interactive/core/procedures/review-checklist.md` | "Core (clean-code.md, both paradigms)" 섹션(155행) | **체크 2항 추가**: ① 좁은 DEC/CURR에 비율산술 금지(BCD 런타임) ② 검산 출력이 값 부재를 0으로 단언 금지. 텍스트만, 링크 불요 | `skills/create-program/phase6-review.md` +2항 |

> 주의: sc4sap의 "Tier-3 triggers"(= `context-loading-protocol.md` 표에 2행)는 하네스에서
> `context-loading-protocol.md`가 **obsolete(어댑터 로딩 계약으로 대체)**라 **1:1 대응물 없음**.
> 하네스의 등가 로딩 계약(어댑터/Context kit)에 신규 2건을 알릴지는 별도 판단 — 본 층2에서
> 필수 아님(페르소나 표 배선으로 dispatch-time 인용은 성립). sc4sap의 "CLAUDE.md index rows"도
> sc4sap common/ 규칙 인덱스용이며, 하네스 루트 `CLAUDE.md`는 프로젝트 정체성 문서라 **직접
> 대응 없음** — 규칙 인덱스 역할은 위 페르소나 표가 수행.

### 5-3. 매니페스트 (`interactive/MIGRATION-MANIFEST.md`)

- **신규 2건 포섭 판정**: 48행 catch-all `| common/** | copy | …conventions/ (규약 18종) |`가
  두 신규 파일을 **이미 포섭**. 48행 위의 특정 규칙 어느 것도 두 파일명을 지정하지 않음 →
  catch-all 매칭. `check-migration-coverage.mjs`는 동결 레포를 `fs.readdirSync`로 **동적
  순회**(SRC=frozen sc4sap-custom)하므로, 드리프트로 생긴 두 파일도 catch-all로 분류됨 →
  **미분류 0 유지, 신규 규칙 불요**.
- **필요 갱신 = 주석 수치만**: `(규약 18종)` → **`(규약 20종)`**. 규칙 자체 불변, 게이트 무관
  (기계는 주석 미계수). 정확성 위한 주석 프레시니스 갱신.

### 5-4. 루트 `CLAUDE.md` 헤드라인 "지식 175"

- **계수 방법**(층1 기준 승계): `interactive/core/knowledge/` 하위 `.md` 파일 수.
  실측 `find interactive/core/knowledge -name '*.md' | wc -l` = **175**(현재, 헤드라인과 일치).
- 신규 2건은 `core/knowledge/abap/conventions/`에 추가되므로 → **이식 후 실측 177**.
- 갱신: 13행 `지식 175` → **`지식 177`**. (페르소나 26·절차 16은 불변 — 페르소나/절차는
  기존 파일 수정만, 신규 파일 없음.)

### 5-5. 파급 갱신 파일 목록(경로)

1. `interactive/core/knowledge/abap/conventions/clean-code.md` (수정)
2. `interactive/core/knowledge/abap/conventions/field-typing-rule.md` (수정)
3. `interactive/core/knowledge/abap/conventions/function-module-rule.md` (수정)
4. `interactive/core/knowledge/abap/conventions/abapgit-roundtrip-rule.md` (신규)
5. `interactive/core/knowledge/abap/conventions/source-repair-protocol.md` (신규)
6. `interactive/core/personas/sap-executor.md` (Shared_Conventions +2행)
7. `interactive/core/personas/sap-debugger.md` (Constraints +1줄)
8. `interactive/core/procedures/create-program.md` (Phase 4 FUGR 레시피)
9. `interactive/core/procedures/review-checklist.md` (Core 체크 +2항)
10. `interactive/MIGRATION-MANIFEST.md` (`18종`→`20종`)
11. `CLAUDE.md` (`지식 175`→`177`)

(+ 문서계약: `HANDOFF.md` 상태·수치 갱신, `docs/DECISIONS.md`는 굵직한 기각 결정 없으면 불요.)

---

## 6. 부록(162~168행, 조건부 5항) 처리 판정

| 부록 항목 | 신판 반영 | 처리 판정 |
|---|---|---|
| abapGit 직렬화(LF·BOM·U01·REMOTE_CALL)는 서버 abapGit 빌드 기준 | `abapgit-roundtrip-rule.md` § Caveat — Re-Verify Per Server에 **내장** | **조건부 표기 그대로 이식**(신규 파일 복사에 포함) |
| SUSH 자동생성·재생성은 S/4 버전 의존 | 동 파일 § Skip SUSH + 재실측 caveat에 **내장** | **이식**(동 파일 복사에 포함) |
| 층1-1 빈 셀 시프트 = 엔진 v4.14.0 수정 | 엔진 층1 항목(층2 지식 아님) | **N-A** — 백로그 5-13 층1(engine 4.13.13)에서 처리 완료 |
| 보호 테이블 승인체계 `acknowledge_risk`는 sc4sap 고유 설계 | 신판 5파일에 추가 안 됨 | **층2 이식 대상 아님(제외)** — 하네스는 자체 `data-extraction-policy` 보유. repo `clean-code.md`에 이미 L1 이월된 `acknowledge_risk` 참조는 그대로 두되 하네스-정본으로 승격하지 않음 |
| 금액 필드 길이확장(DMBTR CURR 23,2·AFLE) 시스템 구성 의존 | 신판 5파일에 없음 | **제외** — 안정 규약이 아닌 시스템 구성 의존 실측 항목. 신판도 미수록. 다중 시스템 중립 하네스에 상수로 실을 근거 없음 |

**요지**: 부록 중 신판 파일에 caveat로 내장된 2건(abapGit·SUSH)은 신규 파일 복사로 자동 이식됨.
나머지 3건은 각각 층1 완료(N-A)·sc4sap 고유(제외)·시스템의존(제외)로 **층2 추가 조치 불요**.

---

## 7. 잔여 리스크

1. **파리티의 범위 해석**: §5-1/5-2(페르소나·절차 배선)를 "층2 필수"로 볼지 "권고"로 볼지는
   실행 세션의 판단 여지. 코어 지식 5파일만 복사하면 파일은 로드 가능하나, 페르소나 표·절차
   체크에 미배선이면 **dispatch 시 인용·자동 트리거가 성립 안 함**(sc4sap는 필수로 배선). 본
   감사는 **페르소나 2·절차 2 배선을 층2 완결 조건으로 권고**.
2. **Tier-3/CLAUDE-index 무대응 지점**: sc4sap가 배선한 `context-loading-protocol` Tier-3 표와
   common/ CLAUDE 인덱스는 하네스에 1:1 대응물이 없다(전자 obsolete·후자 역할 상이). 신규 2건의
   자동 로딩 트리거를 하네스 어댑터 로딩 계약에 넣을지는 **별도 설계 판단**(본 층2 밖). 방치 시
   위험은 낮음(페르소나 표가 lookup 역할 수행).
3. **동결 드리프트 항구성**: 이식 원천이 동결 표기 하 드리프트분(`f7257c0`+`ffb422b`)이다. 향후
   재대조 시 HEAD 재확인 필요 — 본 감사는 HEAD=`ffb422b` 시점 고정. UXX 불릿(`ffb422b`)이
   `f7257c0` 대비 별도 커밋임에 유의(§3-C에 병합 반영).
4. **줄끝(EOL)**: repo conventions는 CRLF, 동결 git-show는 LF 출력. §1 대조는 `--strip-trailing-cr`로
   내용만 비교했다. 신규 2건 복사 시 **repo 기존 규약의 EOL(CRLF)에 맞춰** 저장할 것(레포 일관성).
5. **매니페스트 카운트 자동화 부재**: `(규약 20종)`은 수기 주석 — 기계 검증 대상 아니므로 향후
   추가 이식 때 재드리프트 가능(경미).
