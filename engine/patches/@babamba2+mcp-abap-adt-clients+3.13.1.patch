diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorDefinition/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorDefinition/delete.js
index 52dee5f..c38ff15 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorDefinition/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorDefinition/delete.js
@@ -7,6 +7,7 @@ exports.checkDeletion = checkDeletion;
 exports.deleteBehaviorDefinition = deleteBehaviorDefinition;
 const contentTypes_1 = require("../../constants/contentTypes");
 const timeouts_1 = require("../../utils/timeouts");
+const internalUtils_1 = require("../../utils/internalUtils");
 /**
  * Check if behavior definition can be deleted
  *
@@ -77,11 +78,13 @@ async function deleteBehaviorDefinition(connection, name, transportRequest) {
         'Content-Type': contentTypes_1.CT_DELETION,
     };
     const deletionUrl = `/sap/bc/adt/deletion/delete`;
-    return await connection.makeAdtRequest({
+    const response = await connection.makeAdtRequest({
         url: deletionUrl,
         method: 'POST',
         timeout: (0, timeouts_1.getTimeout)('default'),
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, 'Behavior definition');
+    return response;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorImplementation/AdtBehaviorImplementation.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorImplementation/AdtBehaviorImplementation.js
index 01d3c3d..e0cfe11 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorImplementation/AdtBehaviorImplementation.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/behaviorImplementation/AdtBehaviorImplementation.js
@@ -251,6 +251,15 @@ ENDCLASS.`;
             lockHandle = await this.class.lock({ className: config.className });
             state.lockHandle = lockHandle;
             this.logger?.info?.('Behavior implementation class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the writes:
+            // this.class.lock() returns with the session reset to stateless,
+            // but the inactive check + the two PUTs below (main source and
+            // implementations include) must ride the same stateful session or
+            // connection-recycling backends (e.g. IDES) tear the ADT session
+            // down and the lock evaporates -> HTTP 423 "resource not locked
+            // (invalid lock handle)". The unlock in step 5 (or the error
+            // cleanup) restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Get code for update (from options or config)
             const codeToCheck = options?.sourceCode || config.implementationCode || config.sourceCode;
             // 3. Check inactive version (without sourceCode - implementations include code is not full class code)
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtClass.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtClass.js
index 1e554d3..dc8b3b7 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtClass.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtClass.js
@@ -140,6 +140,8 @@ class AdtClass {
                 create_protected: config.createProtected,
                 master_system: config.masterSystem ?? this.systemContext.masterSystem,
                 responsible: config.responsible ?? this.systemContext.responsible,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                master_language: config.masterLanguage,
                 template_xml: config.classTemplate,
             }, this.logger, this.contentTypes);
             objectCreated = true;
@@ -605,7 +607,12 @@ class AdtClass {
             this.logger?.info?.('Step 1: Locking parent class');
             this.connection.setSessionType('stateful');
             lockHandle = await (0, lock_1.lockClass)(this.connection, config.className);
-            this.connection.setSessionType('stateless');
+            // [powerup 4.13.7] Do NOT reset to stateless here: the PUT below
+            // must ride the same stateful session as the lock or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". The unlock in step 3 (or the error cleanup) restores
+            // stateless.
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
             // 2. Update test classes (uses parent class lock handle)
             this.logger?.info?.('Step 2: Updating test classes');
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalDefinitions.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalDefinitions.js
index ede11ff..7faa6cc 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalDefinitions.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalDefinitions.js
@@ -83,6 +83,13 @@ class AdtLocalDefinitions extends AdtClass_1.AdtClass {
             lockHandle = await super.lock({ className: config.className });
             state.lockHandle = lockHandle;
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check local definitions code
             const codeToCheck = options?.sourceCode || config.definitionsCode;
             if (codeToCheck) {
@@ -178,6 +185,13 @@ class AdtLocalDefinitions extends AdtClass_1.AdtClass {
             lockHandle = await super.lock({ className: config.className });
             state.lockHandle = lockHandle;
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check local definitions code
             const codeToCheck = options?.sourceCode || config.definitionsCode;
             if (codeToCheck) {
@@ -221,11 +235,43 @@ class AdtLocalDefinitions extends AdtClass_1.AdtClass {
         if (!config.className) {
             throw new Error('Class name is required');
         }
-        // Delete by updating with empty code
-        return await this.update({
-            ...config,
-            definitionsCode: '',
-        });
+        // [powerup 4.13.15] Delete = clear the include via a dedicated
+        // empty-payload PUT (backlog 11-⑩). The old body delegated to
+        // update({ definitionsCode: '' }), but update()'s "code is required"
+        // guard rejected the empty payload before the lock, so Delete never
+        // reached the wire. Run our own lock -> (4.13.7 stateful pin) -> clear
+        // PUT -> unlock chain.
+        let lockHandle;
+        try {
+            this.logger?.info?.('Step 1: Locking parent class');
+            lockHandle = await super.lock({ className: config.className });
+            this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write.
+            this.connection.setSessionType('stateful');
+            this.logger?.info?.('Step 2: Clearing local definitions include');
+            const updateResponse = await (0, includes_1.clearClassInclude)(this.connection, config.className, 'definitions', lockHandle, config.transportRequest, this.contentTypes?.sourceArtifactContentType());
+            this.logger?.info?.('Step 3: Unlocking parent class');
+            const unlockState = await super.unlock({ className: config.className }, lockHandle);
+            lockHandle = undefined;
+            return {
+                updateResult: updateResponse,
+                unlockResult: unlockState.unlockResult,
+                errors: [],
+            };
+        }
+        catch (error) {
+            if (lockHandle) {
+                try {
+                    this.logger?.warn?.('Unlocking parent class during error cleanup');
+                    await super.unlock({ className: config.className }, lockHandle);
+                }
+                catch (unlockError) {
+                    this.logger?.warn?.('Failed to unlock parent class after error:', (0, internalUtils_1.safeErrorMessage)(unlockError));
+                }
+            }
+            this.logger?.error('Delete LocalDefinitions failed:', (0, internalUtils_1.safeErrorMessage)(error));
+            throw error;
+        }
     }
     /**
      * Activate parent class (local definitions are activated with parent class)
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalMacros.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalMacros.js
index 24f19c0..db03391 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalMacros.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalMacros.js
@@ -84,6 +84,13 @@ class AdtLocalMacros extends AdtClass_1.AdtClass {
             lockHandle = await super.lock({ className: config.className });
             state.lockHandle = lockHandle;
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check local macros code
             const codeToCheck = options?.sourceCode || config.macrosCode;
             if (codeToCheck) {
@@ -179,6 +186,13 @@ class AdtLocalMacros extends AdtClass_1.AdtClass {
             lockHandle = await super.lock({ className: config.className });
             state.lockHandle = lockHandle;
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check local macros code
             const codeToCheck = options?.sourceCode || config.macrosCode;
             if (codeToCheck) {
@@ -222,11 +236,43 @@ class AdtLocalMacros extends AdtClass_1.AdtClass {
         if (!config.className) {
             throw new Error('Class name is required');
         }
-        // Delete by updating with empty code
-        return await this.update({
-            ...config,
-            macrosCode: '',
-        });
+        // [powerup 4.13.15] Delete = clear the include via a dedicated
+        // empty-payload PUT (backlog 11-⑩). The old body delegated to
+        // update({ macrosCode: '' }), but update()'s "code is required" guard
+        // rejected the empty payload before the lock, so Delete never reached
+        // the wire. Run our own lock -> (4.13.7 stateful pin) -> clear PUT ->
+        // unlock chain.
+        let lockHandle;
+        try {
+            this.logger?.info?.('Step 1: Locking parent class');
+            lockHandle = await super.lock({ className: config.className });
+            this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write.
+            this.connection.setSessionType('stateful');
+            this.logger?.info?.('Step 2: Clearing local macros include');
+            const updateResponse = await (0, includes_1.clearClassInclude)(this.connection, config.className, 'macros', lockHandle, config.transportRequest, this.contentTypes?.sourceArtifactContentType());
+            this.logger?.info?.('Step 3: Unlocking parent class');
+            const unlockState = await super.unlock({ className: config.className }, lockHandle);
+            lockHandle = undefined;
+            return {
+                updateResult: updateResponse,
+                unlockResult: unlockState.unlockResult,
+                errors: [],
+            };
+        }
+        catch (error) {
+            if (lockHandle) {
+                try {
+                    this.logger?.warn?.('Unlocking parent class during error cleanup');
+                    await super.unlock({ className: config.className }, lockHandle);
+                }
+                catch (unlockError) {
+                    this.logger?.warn?.('Failed to unlock parent class after error:', (0, internalUtils_1.safeErrorMessage)(unlockError));
+                }
+            }
+            this.logger?.error('Delete LocalMacros failed:', (0, internalUtils_1.safeErrorMessage)(error));
+            throw error;
+        }
     }
     /**
      * Activate parent class (local macros are activated with parent class)
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTestClass.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTestClass.js
index 80bfe03..c3b9c99 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTestClass.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTestClass.js
@@ -84,6 +84,13 @@ class AdtLocalTestClass extends AdtClass_1.AdtClass {
             parentLockHandle = await this.lock({ className: config.className });
             state.lockHandle = parentLockHandle;
             this.logger?.info?.('Parent class locked, handle:', parentLockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check test class code
             if (config.testClassCode) {
                 this.logger?.info?.('Step 2: Checking test class code');
@@ -190,6 +197,13 @@ class AdtLocalTestClass extends AdtClass_1.AdtClass {
             parentLockHandle = await this.lock({ className: config.className });
             state.lockHandle = parentLockHandle;
             this.logger?.info?.('Parent class locked, handle:', parentLockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check test class code
             const codeToCheck = options?.sourceCode || config.testClassCode;
             if (codeToCheck) {
@@ -244,11 +258,43 @@ class AdtLocalTestClass extends AdtClass_1.AdtClass {
         if (!config.className) {
             throw new Error('Class name is required');
         }
-        // Delete by updating with empty code
-        return await this.update({
-            ...config,
-            testClassCode: '',
-        });
+        // [powerup 4.13.15] Delete = clear the testclasses include via a
+        // dedicated empty-payload PUT (backlog 11-⑩). The old body delegated to
+        // update({ testClassCode: '' }), but update()'s "code is required" guard
+        // rejected the empty payload before the lock, so Delete never reached
+        // the wire. Run our own lock -> (4.13.7 stateful pin) -> clear PUT ->
+        // unlock chain.
+        let parentLockHandle;
+        try {
+            this.logger?.info?.('Step 1: Locking parent class');
+            parentLockHandle = await this.lock({ className: config.className });
+            this.logger?.info?.('Parent class locked, handle:', parentLockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write.
+            this.connection.setSessionType('stateful');
+            this.logger?.info?.('Step 2: Clearing test classes include');
+            const updateResponse = await (0, testclasses_1.clearClassTestInclude)(this.connection, config.className, parentLockHandle, config.transportRequest, this.contentTypes?.sourceArtifactContentType());
+            this.logger?.info?.('Step 3: Unlocking parent class');
+            const unlockState = await super.unlock({ className: config.className }, parentLockHandle);
+            parentLockHandle = undefined;
+            return {
+                updateResult: updateResponse,
+                unlockResult: unlockState.unlockResult,
+                errors: [],
+            };
+        }
+        catch (error) {
+            if (parentLockHandle) {
+                try {
+                    this.logger?.warn?.('Unlocking parent class during error cleanup');
+                    await super.unlock({ className: config.className }, parentLockHandle);
+                }
+                catch (unlockError) {
+                    this.logger?.warn?.('Failed to unlock parent class after error:', (0, internalUtils_1.safeErrorMessage)(unlockError));
+                }
+            }
+            this.logger?.error('Delete LocalTestClass failed:', (0, internalUtils_1.safeErrorMessage)(error));
+            throw error;
+        }
     }
     /**
      * Check local test class code
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTypes.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTypes.js
index b6387fa..67b26e6 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTypes.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/AdtLocalTypes.js
@@ -79,6 +79,13 @@ class AdtLocalTypes extends AdtClass_1.AdtClass {
             this.logger?.info?.('Step 1: Locking parent class');
             lockHandle = await super.lock({ className: config.className });
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check local types code
             const codeToCheck = options?.sourceCode || config.localTypesCode;
             const state = {
@@ -173,6 +180,13 @@ class AdtLocalTypes extends AdtClass_1.AdtClass {
             this.logger?.info?.('Step 1: Locking parent class');
             lockHandle = await super.lock({ className: config.className });
             this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write:
+            // lock() returns with the session reset to stateless, but the check
+            // + PUT below must ride the same stateful session or connection-
+            // recycling backends (e.g. IDES) tear the ADT session down and the
+            // lock evaporates -> HTTP 423 "resource not locked (invalid lock
+            // handle)". unlock() restores stateless.
+            this.connection.setSessionType('stateful');
             // 2. Check local types code
             const codeToCheck = options?.sourceCode || config.localTypesCode;
             const state = {
@@ -219,11 +233,43 @@ class AdtLocalTypes extends AdtClass_1.AdtClass {
         if (!config.className) {
             throw new Error('Class name is required');
         }
-        // Delete by updating with empty code
-        return await this.update({
-            ...config,
-            localTypesCode: '',
-        });
+        // [powerup 4.13.15] Delete = clear the include via a dedicated
+        // empty-payload PUT (backlog 11-⑩). The old body delegated to
+        // update({ localTypesCode: '' }), but update()'s "code is required"
+        // guard rejected the empty payload before the lock, so Delete never
+        // reached the wire. Run our own lock -> (4.13.7 stateful pin) -> clear
+        // PUT -> unlock chain.
+        let lockHandle;
+        try {
+            this.logger?.info?.('Step 1: Locking parent class');
+            lockHandle = await super.lock({ className: config.className });
+            this.logger?.info?.('Parent class locked, handle:', lockHandle);
+            // [powerup 4.13.7] Keep the ENQUEUE lock alive through the write.
+            this.connection.setSessionType('stateful');
+            this.logger?.info?.('Step 2: Clearing local types include');
+            const updateResponse = await (0, includes_1.clearClassInclude)(this.connection, config.className, 'implementations', lockHandle, config.transportRequest, this.contentTypes?.sourceArtifactContentType());
+            this.logger?.info?.('Step 3: Unlocking parent class');
+            const unlockState = await super.unlock({ className: config.className }, lockHandle);
+            lockHandle = undefined;
+            return {
+                updateResult: updateResponse,
+                unlockResult: unlockState.unlockResult,
+                errors: [],
+            };
+        }
+        catch (error) {
+            if (lockHandle) {
+                try {
+                    this.logger?.warn?.('Unlocking parent class during error cleanup');
+                    await super.unlock({ className: config.className }, lockHandle);
+                }
+                catch (unlockError) {
+                    this.logger?.warn?.('Failed to unlock parent class after error:', (0, internalUtils_1.safeErrorMessage)(unlockError));
+                }
+            }
+            this.logger?.error('Delete LocalTypes failed:', (0, internalUtils_1.safeErrorMessage)(error));
+            throw error;
+        }
     }
     /**
      * Activate parent class (local types are activated with parent class)
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/create.js
index c393a46..0de6aa2 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/create.js
@@ -35,7 +35,13 @@ async function create(connection, args, logger, contentTypes) {
     const templateSection = args.template_xml
         ? `\n\n  ${args.template_xml}\n\n`
         : '\n\n';
-    const metadataXml = `<?xml version="1.0" encoding="UTF-8"?><class:abapClass xmlns:class="http://www.sap.com/adt/oo/classes" xmlns:adtcore="http://www.sap.com/adt/core"${abapSourceNamespace} adtcore:description="${description}" adtcore:language="EN" adtcore:name="${args.class_name}" adtcore:type="CLAS/OC" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr} class:final="${finalAttr}" class:visibility="${visibilityAttr}">
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (args.master_language || 'EN').toUpperCase();
+    const metadataXml = `<?xml version="1.0" encoding="UTF-8"?><class:abapClass xmlns:class="http://www.sap.com/adt/oo/classes" xmlns:adtcore="http://www.sap.com/adt/core"${abapSourceNamespace} adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${args.class_name}" adtcore:type="CLAS/OC" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr} class:final="${finalAttr}" class:visibility="${visibilityAttr}">
 
 
 
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/delete.js
index 7f00cad..b29a42c 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/delete.js
@@ -71,6 +71,7 @@ async function deleteClass(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Class");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/includes.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/includes.js
index f454778..f010472 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/includes.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/includes.js
@@ -6,6 +6,7 @@ Object.defineProperty(exports, "__esModule", { value: true });
 exports.updateClassLocalTypes = updateClassLocalTypes;
 exports.updateClassDefinitions = updateClassDefinitions;
 exports.updateClassMacros = updateClassMacros;
+exports.clearClassInclude = clearClassInclude;
 const contentTypes_1 = require("../../constants/contentTypes");
 const internalUtils_1 = require("../../utils/internalUtils");
 const timeouts_1 = require("../../utils/timeouts");
@@ -94,3 +95,43 @@ async function updateClassInclude(connection, className, includeSource, includeT
         headers,
     });
 }
+// [powerup 4.13.15] Effectively-empty include source for the delete/clear path.
+// S/4HANA normalises a single space to an empty include (verified live on
+// /includes/implementations and /includes/testclasses); a literal '' trips the
+// falsy guards that made Delete unreachable.
+const EMPTY_INCLUDE_SOURCE = ' ';
+/**
+ * Clear a class include (delete-only path) — powerup 4.13.15.
+ *
+ * DeleteLocal{Types,Definitions,Macros} used to delegate to
+ * update({ ...code: '' }), but the "code is required" falsy guard on
+ * update()/updateClassInclude rejected the empty payload BEFORE the lock, so
+ * the delete PUT never reached the wire (backlog 11-⑩). This is the dedicated
+ * clear path: it PUTs an effectively-empty source to the include endpoint.
+ *
+ * The common `if (!includeSource)` guard on updateClassInclude is intentionally
+ * left intact so a normal Update with empty code is still rejected (blast-radius
+ * containment). Requires the class to be locked (lock handle) before calling.
+ */
+async function clearClassInclude(connection, className, includeType, lockHandle, transportRequest, sourceContentType) {
+    if (!lockHandle) {
+        throw new Error(`lockHandle is required to clear ${includeType}`);
+    }
+    const encodedName = (0, internalUtils_1.encodeSapObjectName)(className).toLowerCase();
+    let url = `/sap/bc/adt/oo/classes/${encodedName}/includes/${includeType}?lockHandle=${encodeURIComponent(lockHandle)}`;
+    if (transportRequest) {
+        url += `&corrNr=${transportRequest}`;
+    }
+    const contentType = sourceContentType || contentTypes_1.CT_SOURCE;
+    const headers = {
+        'Content-Type': contentType,
+        Accept: contentTypes_1.ACCEPT_SOURCE,
+    };
+    return await connection.makeAdtRequest({
+        url,
+        method: 'PUT',
+        timeout: (0, timeouts_1.getTimeout)('default'),
+        data: EMPTY_INCLUDE_SOURCE,
+        headers,
+    });
+}
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/testclasses.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/testclasses.js
index d03cd38..80ad618 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/testclasses.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/testclasses.js
@@ -4,6 +4,7 @@
  */
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.updateClassTestInclude = updateClassTestInclude;
+exports.clearClassTestInclude = clearClassTestInclude;
 exports.lockClassTestClasses = lockClassTestClasses;
 exports.unlockClassTestClasses = unlockClassTestClasses;
 exports.activateClassTestClasses = activateClassTestClasses;
@@ -41,6 +42,42 @@ async function updateClassTestInclude(connection, className, testClassSource, lo
         headers,
     });
 }
+// [powerup 4.13.15] Effectively-empty testclasses source for the delete/clear
+// path — a single space, which S/4HANA normalises to an empty include
+// (verified live). See clearClassInclude in ./includes for the full rationale.
+const EMPTY_TESTCLASS_SOURCE = ' ';
+/**
+ * Clear a class testclasses include (delete-only path) — powerup 4.13.15.
+ *
+ * DeleteLocalTestClass used to delegate to update({ testClassCode: '' }), whose
+ * "code is required" falsy guard rejected the empty payload before the lock, so
+ * the delete PUT never reached the wire (backlog 11-⑩). This dedicated clear
+ * path PUTs an effectively-empty source. The `if (!testClassSource)` guard on
+ * updateClassTestInclude is left intact so a normal Update with empty code is
+ * still rejected. Requires the class to be locked (lock handle) before calling.
+ */
+async function clearClassTestInclude(connection, className, lockHandle, transportRequest, sourceContentType) {
+    if (!lockHandle) {
+        throw new Error('lockHandle is required to clear test classes');
+    }
+    const encodedName = (0, internalUtils_1.encodeSapObjectName)(className).toLowerCase();
+    let url = `/sap/bc/adt/oo/classes/${encodedName}/includes/testclasses?lockHandle=${encodeURIComponent(lockHandle)}`;
+    if (transportRequest) {
+        url += `&corrNr=${transportRequest}`;
+    }
+    const contentType = sourceContentType || contentTypes_1.CT_SOURCE;
+    const headers = {
+        'Content-Type': contentType,
+        Accept: contentTypes_1.ACCEPT_SOURCE,
+    };
+    return await connection.makeAdtRequest({
+        url,
+        method: 'PUT',
+        timeout: (0, timeouts_1.getTimeout)('default'),
+        data: EMPTY_TESTCLASS_SOURCE,
+        headers,
+    });
+}
 async function lockClassTestClasses(connection, className) {
     const encodedName = (0, internalUtils_1.encodeSapObjectName)(className).toLowerCase();
     const url = `/sap/bc/adt/oo/classes/${encodedName}/includes/testclasses?_action=LOCK&accessMode=MODIFY`;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/types.d.ts
index 21cf2c4..91c8176 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/class/types.d.ts
@@ -8,6 +8,7 @@ export interface ICreateClassParams {
     package_name: string;
     transport_request?: string;
     master_system?: string;
+    master_language?: string;
     responsible?: string;
     superclass?: string;
     final?: boolean;
@@ -35,6 +36,7 @@ export interface IClassConfig {
     abstract?: boolean;
     createProtected?: boolean;
     masterSystem?: string;
+    masterLanguage?: string;
     responsible?: string;
     classTemplate?: string;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/AdtDataElement.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/AdtDataElement.js
index 2e26c46..ecf8275 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/AdtDataElement.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/AdtDataElement.js
@@ -95,6 +95,7 @@ class AdtDataElement {
                 set_get_parameter: config.setGetParameter,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                master_language: config.masterLanguage,
             });
             state.createResult = createResponse;
             objectCreated = true;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/create.js
index 25fbc29..512ea70 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/create.js
@@ -83,16 +83,24 @@ async function create(connection, args) {
     const masterSystemAttr = masterSystem
         ? ` adtcore:masterSystem="${masterSystem}"`
         : '';
+    // [powerup 4.13.10] The payload language must match the system's logon /
+    // master language or SAP drops the description from the created object
+    // (leaving a skeleton whose GET XML has no adtcore:description at all —
+    // HANDOFF §6 backlog 11-⑧, live-measured on an S/4HANA system logged on
+    // in CS). Callers resolve the logon language from the live system and
+    // pass it as master_language; the historical EN default is kept as the
+    // fallback when it is not provided.
+    const masterLanguage = (args.master_language || 'EN').toUpperCase();
     const xmlBody = `<?xml version="1.0" encoding="UTF-8"?>
 <blue:wbobj xmlns:blue="http://www.sap.com/wbobj/dictionary/dtel"
             xmlns:adtcore="http://www.sap.com/adt/core"
             xmlns:atom="http://www.w3.org/2005/Atom"
             xmlns:dtel="http://www.sap.com/adt/dictionary/dataelements"
             adtcore:description="${description}"
-            adtcore:language="EN"
+            adtcore:language="${masterLanguage}"
             adtcore:name="${args.data_element_name.toUpperCase()}"
             adtcore:type="DTEL/DE"
-            adtcore:masterLanguage="EN"${masterSystemAttr}
+            adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}
             adtcore:responsible="${username}">
   <adtcore:packageRef adtcore:name="${args.package_name.toUpperCase()}"/>
   <dtel:dataElement>
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/delete.js
index 19f115d..6d2833d 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/delete.js
@@ -71,6 +71,7 @@ async function deleteDataElement(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Data element");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/types.d.ts
index f15ad77..6a92b19 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/types.d.ts
@@ -20,6 +20,7 @@ export interface ICreateDataElementParams {
     transport_request?: string;
     masterSystem?: string;
     responsible?: string;
+    master_language?: string;
     type_kind?: 'domain' | 'predefinedAbapType' | 'refToPredefinedAbapType' | 'refToDictionaryType' | 'refToClifType';
     type_name?: string;
     data_type?: string;
@@ -71,6 +72,7 @@ export interface IDataElementConfig {
     packageName?: string;
     transportRequest?: string;
     description?: string;
+    masterLanguage?: string;
     dataType?: string;
     length?: number;
     decimals?: number;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/update.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/update.js
index 53f04a3..b5e478e 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/update.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/dataElement/update.js
@@ -84,10 +84,13 @@ async function getDomainInfo(connection, domainName) {
  */
 function patchDataElementXml(currentXml, args) {
     let xml = currentXml;
-    // Description
+    // Description — addIfMissing so a half-created skeleton whose GET XML
+    // carries no adtcore:description at all (EN-payload create on a non-EN
+    // logon system, HANDOFF §6 backlog 11-⑧) is repairable via Update
+    // instead of failing the PUT with "Die Beschreibung fehlt".
     if (args.description) {
         const description = (0, internalUtils_1.limitDescription)(args.description);
-        xml = (0, xmlPatch_1.patchXmlAttribute)(xml, 'adtcore:description', description);
+        xml = (0, xmlPatch_1.patchXmlAttribute)(xml, 'adtcore:description', description, { addIfMissing: true });
     }
     // Type information
     xml = (0, xmlPatch_1.patchIf)(xml, args.type_kind, (x, val) => (0, xmlPatch_1.patchXmlElement)(x, 'dtel:typeKind', val));
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/AdtDomain.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/AdtDomain.js
index 4e6ae55..666c449 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/AdtDomain.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/AdtDomain.js
@@ -88,6 +88,7 @@ class AdtDomain {
                 fixed_values: config.fixed_values,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                master_language: config.masterLanguage,
             });
             state.createResult = createResponse;
             objectCreated = true;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/create.js
index 7738290..51461b4 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/create.js
@@ -23,14 +23,22 @@ async function create(connection, args) {
         ? ` adtcore:masterSystem="${masterSystem}"`
         : '';
     const responsibleAttr = username ? ` adtcore:responsible="${username}"` : '';
+    // [powerup 4.13.10] The payload language must match the system's logon /
+    // master language or SAP drops the description from the created object
+    // (leaving a skeleton whose GET XML has no adtcore:description at all —
+    // HANDOFF §6 backlog 11-⑧, live-measured on an S/4HANA system logged on
+    // in CS). Callers resolve the logon language from the live system and
+    // pass it as master_language; the historical EN default is kept as the
+    // fallback when it is not provided.
+    const masterLanguage = (args.master_language || 'EN').toUpperCase();
     const xmlBody = `<?xml version="1.0" encoding="UTF-8"?>
 <doma:domain xmlns:doma="http://www.sap.com/dictionary/domain"
              xmlns:adtcore="http://www.sap.com/adt/core"
              adtcore:description="${args.description || args.domain_name}"
-             adtcore:language="EN"
+             adtcore:language="${masterLanguage}"
              adtcore:name="${args.domain_name.toUpperCase()}"
              adtcore:type="DOMA/DD"
-             adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr}>
+             adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
   <adtcore:packageRef adtcore:name="${args.package_name.toUpperCase()}"/>
 </doma:domain>`;
     const headers = {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/delete.js
index 8c88ee4..84b74bb 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/delete.js
@@ -71,6 +71,7 @@ async function deleteDomain(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Domain");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/types.d.ts
index 26e3621..308d9f4 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/types.d.ts
@@ -13,6 +13,7 @@ export interface ICreateDomainParams {
     transport_request?: string;
     masterSystem?: string;
     responsible?: string;
+    master_language?: string;
     datatype?: string;
     length?: number;
     decimals?: number;
@@ -49,6 +50,7 @@ export interface IDomainConfig {
     packageName?: string;
     transportRequest?: string;
     description?: string;
+    masterLanguage?: string;
     datatype?: string;
     length?: number;
     decimals?: number;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/update.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/update.js
index 07580d1..baf0e0d 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/update.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/domain/update.js
@@ -17,10 +17,13 @@ const xmlPatch_1 = require("../../utils/xmlPatch");
  */
 function patchDomainXml(currentXml, args) {
     let xml = currentXml;
-    // Description
+    // Description — addIfMissing so a half-created skeleton whose GET XML
+    // carries no adtcore:description at all (EN-payload create on a non-EN
+    // logon system, HANDOFF §6 backlog 11-⑧) is repairable via Update
+    // instead of failing the PUT with "Die Beschreibung fehlt".
     if (args.description) {
         const description = (0, internalUtils_1.limitDescription)(args.description);
-        xml = (0, xmlPatch_1.patchXmlAttribute)(xml, 'adtcore:description', description);
+        xml = (0, xmlPatch_1.patchXmlAttribute)(xml, 'adtcore:description', description, { addIfMissing: true });
     }
     // Type information
     xml = (0, xmlPatch_1.patchIf)(xml, args.datatype, (x, val) => (0, xmlPatch_1.patchXmlElement)(x, 'doma:datatype', val));
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionGroup/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionGroup/delete.js
index e1a7813..bd7ea59 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionGroup/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionGroup/delete.js
@@ -71,6 +71,7 @@ async function deleteFunctionGroup(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Function group");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionModule/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionModule/delete.js
index de4fe06..7f8db2c 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionModule/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/functionModule/delete.js
@@ -79,6 +79,7 @@ async function deleteFunctionModule(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Function module");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/AdtInterface.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/AdtInterface.js
index d68a8a9..585b483 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/AdtInterface.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/AdtInterface.js
@@ -82,6 +82,8 @@ class AdtInterface {
                 description: config.description,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                masterLanguage: config.masterLanguage,
             }, this.logger);
             state.createResult = createResponse;
             objectCreated = true;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/create.js
index 2d4cf18..120f35e 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/create.js
@@ -39,7 +39,13 @@ async function create(connection, params, logger) {
     const responsibleAttr = finalResponsible
         ? ` adtcore:responsible="${finalResponsible}"`
         : '';
-    const payload = `<?xml version="1.0" encoding="UTF-8"?><intf:abapInterface xmlns:intf="http://www.sap.com/adt/oo/interfaces" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${limitedDescription}" adtcore:language="EN" adtcore:name="${params.interfaceName}" adtcore:type="INTF/OI" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr}>
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (params.masterLanguage || 'EN').toUpperCase();
+    const payload = `<?xml version="1.0" encoding="UTF-8"?><intf:abapInterface xmlns:intf="http://www.sap.com/adt/oo/interfaces" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${limitedDescription}" adtcore:language="${masterLanguage}" adtcore:name="${params.interfaceName}" adtcore:type="INTF/OI" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
 
 
 
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/delete.js
index 62b1b54..d9acdd1 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/delete.js
@@ -71,6 +71,7 @@ async function deleteInterface(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Interface");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/types.d.ts
index 2766c55..802d34d 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/interface/types.d.ts
@@ -8,6 +8,7 @@ export interface ICreateInterfaceParams {
     packageName: string;
     transportRequest?: string;
     masterSystem?: string;
+    masterLanguage?: string;
     responsible?: string;
 }
 export interface IUpdateInterfaceSourceParams {
@@ -25,6 +26,7 @@ export interface IInterfaceConfig extends IAdtObjectConfig {
     transportRequest?: string;
     description?: string;
     sourceCode?: string;
+    masterLanguage?: string;
     sessionId?: string;
     onLock?: (lockHandle: string) => void;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/metadataExtension/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/metadataExtension/create.js
index 69b9a34..89f2e9b 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/metadataExtension/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/metadataExtension/create.js
@@ -29,6 +29,10 @@ const timeouts_1 = require("../../utils/timeouts");
  */
 async function createMetadataExtension(connection, params) {
     const url = '/sap/bc/adt/ddic/ddlx/sources';
+    // [powerup 4.13.12] masterLanguage already flowed into adtcore:masterLanguage,
+    // but adtcore:language was still hardcoded EN below — so on a non-EN logon
+    // system the description landed in the EN text slot and read back empty
+    // (HANDOFF §6 backlog 11-⑫). Both attributes now use the resolved language.
     const masterLanguage = params.masterLanguage || 'EN';
     const masterSystem = params.masterSystem || '';
     const responsible = params.responsible || '';
@@ -41,7 +45,7 @@ async function createMetadataExtension(connection, params) {
     const responsibleAttr = responsible
         ? ` adtcore:responsible="${responsible}"`
         : '';
-    const xmlBody = `<?xml version="1.0" encoding="UTF-8"?><ddlxsources:ddlxSource xmlns:ddlxsources="http://www.sap.com/adt/ddic/ddlxsources" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="EN" adtcore:name="${params.name}" adtcore:type="DDLX/EX" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
+    const xmlBody = `<?xml version="1.0" encoding="UTF-8"?><ddlxsources:ddlxSource xmlns:ddlxsources="http://www.sap.com/adt/ddic/ddlxsources" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${params.name}" adtcore:type="DDLX/EX" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
     ${params.transportRequest
         ? `<adtcore:packageRef adtcore:name="${params.packageName}">
     <adtcore:properties>
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/AdtPackage.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/AdtPackage.js
index 2fc66dc..9270257 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/AdtPackage.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/AdtPackage.js
@@ -116,6 +116,8 @@ class AdtPackage {
                 application_component: config.applicationComponent,
                 responsible: config.responsible ?? this.systemContext.responsible,
                 master_system: this.systemContext.masterSystem,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                master_language: config.masterLanguage,
                 record_changes: config.recordChanges ?? false,
             });
             this.logger?.info?.('Package created');
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/create.js
index 05fcb99..98fe85f 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/create.js
@@ -46,8 +46,14 @@ async function createPackage(connection, params) {
     const masterSystemAttr = masterSystem
         ? ` adtcore:masterSystem="${escapeXml(masterSystem)}"`
         : '';
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (params.master_language || 'EN').toUpperCase();
     const xmlBody = `<?xml version="1.0" encoding="UTF-8"?>
-<pak:package xmlns:pak="http://www.sap.com/adt/packages" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="EN" adtcore:name="${params.package_name}" adtcore:type="DEVC/K" adtcore:version="active" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr}>
+<pak:package xmlns:pak="http://www.sap.com/adt/packages" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${params.package_name}" adtcore:type="DEVC/K" adtcore:version="active" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
   <adtcore:packageRef adtcore:name="${params.package_name}"/>
   <pak:attributes pak:isEncapsulated="false" pak:packageType="${packageType}" pak:recordChanges="${params.record_changes ? 'true' : 'false'}"/>
   ${superPackageXml}
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/types.d.ts
index 32d97f9..0ad86a1 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/package/types.d.ts
@@ -15,6 +15,7 @@ export interface IPackageConfig {
     applicationComponent?: string;
     responsible?: string;
     masterSystem?: string;
+    masterLanguage?: string;
     recordChanges?: boolean;
     onLock?: (lockHandle: string) => void;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/AdtProgram.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/AdtProgram.js
index 2c9bf96..38dc18a 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/AdtProgram.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/AdtProgram.js
@@ -101,6 +101,8 @@ class AdtProgram {
                 sourceCode: options?.sourceCode || config.sourceCode,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                masterLanguage: config.masterLanguage,
             }, this.contentTypes);
             state.createResult = createResponse;
             objectCreated = true;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/create.js
index be24461..ba800ae 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/create.js
@@ -71,7 +71,13 @@ async function create(connection, args, contentTypes) {
         ? ` adtcore:masterSystem="${masterSystem}"`
         : '';
     const responsibleAttr = username ? ` adtcore:responsible="${username}"` : '';
-    const metadataXml = `<?xml version="1.0" encoding="UTF-8"?><program:abapProgram xmlns:program="http://www.sap.com/adt/programs/programs" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="EN" adtcore:name="${args.programName}" adtcore:type="PROG/P" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr} program:programType="${programType}" program:application="${application}">
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (args.masterLanguage || 'EN').toUpperCase();
+    const metadataXml = `<?xml version="1.0" encoding="UTF-8"?><program:abapProgram xmlns:program="http://www.sap.com/adt/programs/programs" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${args.programName}" adtcore:type="PROG/P" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr} program:programType="${programType}" program:application="${application}">
   <adtcore:packageRef adtcore:name="${args.packageName}"/>
 </program:abapProgram>`;
     const ct = contentTypes?.programCreate();
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/delete.js
index 0f94ca0..d681e37 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/delete.js
@@ -70,6 +70,7 @@ async function deleteProgram(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Program");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/types.d.ts
index 4adfcc8..60300de 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/program/types.d.ts
@@ -8,6 +8,7 @@ export interface ICreateProgramParams {
     packageName: string;
     transportRequest?: string;
     masterSystem?: string;
+    masterLanguage?: string;
     responsible?: string;
     programType?: string;
     application?: string;
@@ -31,6 +32,7 @@ export interface IProgramConfig {
     programType?: string;
     application?: string;
     sourceCode?: string;
+    masterLanguage?: string;
     sessionId?: string;
     onLock?: (lockHandle: string) => void;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/AdtServiceDefinition.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/AdtServiceDefinition.js
index c39ee4e..ef8eb43 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/AdtServiceDefinition.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/AdtServiceDefinition.js
@@ -93,6 +93,8 @@ class AdtServiceDefinition {
                 source_code: options?.sourceCode || config.sourceCode,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                masterLanguage: config.masterLanguage,
             });
             state.createResult = createResponse;
             this.logger?.info?.('Service definition created');
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/create.js
index 17931d4..d410806 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/create.js
@@ -22,7 +22,13 @@ async function create(connection, args) {
     const masterSystemAttr = masterSystem
         ? ` adtcore:masterSystem="${masterSystem}"`
         : '';
-    const xmlBody = `<?xml version="1.0" encoding="UTF-8"?><srvd:srvdSource xmlns:srvd="http://www.sap.com/adt/ddic/srvdsources" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="EN" adtcore:name="${serviceDefinitionName}" adtcore:type="SRVD/SRV" adtcore:masterLanguage="EN"${masterSystemAttr} adtcore:responsible="${username}" srvd:srvdSourceType="S">
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (args.masterLanguage || 'EN').toUpperCase();
+    const xmlBody = `<?xml version="1.0" encoding="UTF-8"?><srvd:srvdSource xmlns:srvd="http://www.sap.com/adt/ddic/srvdsources" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${serviceDefinitionName}" adtcore:type="SRVD/SRV" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr} adtcore:responsible="${username}" srvd:srvdSourceType="S">
   <adtcore:packageRef adtcore:name="${args.package_name.toUpperCase()}"/>
 </srvd:srvdSource>`;
     const headers = {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/delete.js
index 981064a..55a658c 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/delete.js
@@ -71,6 +71,7 @@ async function deleteServiceDefinition(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Service definition");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/types.d.ts
index 264bcb4..a93f720 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/serviceDefinition/types.d.ts
@@ -9,6 +9,7 @@ export interface ICreateServiceDefinitionParams {
     transport_request?: string;
     source_code?: string;
     masterSystem?: string;
+    masterLanguage?: string;
     responsible?: string;
 }
 export interface IUpdateServiceDefinitionParams {
@@ -26,6 +27,7 @@ export interface IServiceDefinitionConfig {
     transportRequest?: string;
     description?: string;
     sourceCode?: string;
+    masterLanguage?: string;
 }
 export interface IServiceDefinitionState extends IAdtObjectState {
     readSourceResult?: IAdtResponse;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/AdtStructure.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/AdtStructure.js
index 4d5334b..e7bddb5 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/AdtStructure.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/AdtStructure.js
@@ -91,6 +91,8 @@ class AdtStructure {
                 description: config.description,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                masterLanguage: config.masterLanguage,
             });
             objectCreated = true;
             state.createResult = createResponse;
@@ -406,8 +408,19 @@ class AdtStructure {
         }
         // Map status to version
         const version = status === 'active' ? 'active' : 'inactive';
+        // [powerup 4.13.11] Forward config.ddlCode as the source to validate.
+        // Callers that pass ddlCode (the UpdateStructure high-level pre-check
+        // and the CheckStructure low-level tool's documented ddl_code path)
+        // intend to validate the NEW/unsaved code, not whatever version is
+        // already stored on the server. Dropping it silently made the "check
+        // new code" step validate the wrong version: when the stored inactive
+        // version is valid (or absent) the real syntax error in the new DDL was
+        // never surfaced, so the subsequent write PUT failed opaquely ("Kein
+        // Sichern wegen Fehler in Quelle. Details erhalten Sie mit Prüfung." —
+        // error in source, run a check for details). undefined (no ddlCode)
+        // preserves the previous saved-version behavior for the other callers.
         return {
-            checkResult: await (0, check_1.checkStructure)(this.connection, config.structureName, version, undefined, this.logger),
+            checkResult: await (0, check_1.checkStructure)(this.connection, config.structureName, version, config.ddlCode, this.logger),
             errors: [],
         };
     }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/check.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/check.js
index 8a0661e..8c07f6e 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/check.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/check.js
@@ -28,8 +28,21 @@ async function checkStructure(connection, structureName, version = 'active', sou
             }
             return response; // Return response anyway
         }
-        const errorMessages = checkResult.errors.map((err) => err.text).join('; ');
-        throw new Error(`Structure check failed: ${errorMessages}`);
+        // [powerup 4.13.11] Never throw a bare "Structure check failed:" with
+        // no detail. A check can report has_errors while carrying no message
+        // texts — e.g. status="notProcessed" on an object whose requested
+        // version cannot be validated (an empty/absent inactive version) yields
+        // an empty errors list — which produced a deterministic *empty* error
+        // that told the caller nothing. Keep only non-empty texts, and when
+        // none remain fall back to the check status/statusText so the thrown
+        // message is always actionable.
+        const errorMessages = checkResult.errors
+            .map((err) => err.text)
+            .filter((text) => text && String(text).trim())
+            .join('; ');
+        const detail = errorMessages ||
+            `${checkResult.message || ''} (check status: ${checkResult.status})`.trim();
+        throw new Error(`Structure check failed: ${detail}`);
     }
     return response;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/create.js
index bf8043c..b47a699 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/create.js
@@ -24,7 +24,13 @@ async function create(connection, params) {
     const responsibleAttr = responsible
         ? ` adtcore:responsible="${responsible}"`
         : '';
-    const structureXml = `<?xml version="1.0" encoding="UTF-8"?><blue:blueSource xmlns:blue="http://www.sap.com/wbobj/blue" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${limitedDescription}" adtcore:language="EN" adtcore:name="${params.structureName.toUpperCase()}" adtcore:type="TABL/DS" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr}>
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (params.masterLanguage || 'EN').toUpperCase();
+    const structureXml = `<?xml version="1.0" encoding="UTF-8"?><blue:blueSource xmlns:blue="http://www.sap.com/wbobj/blue" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${limitedDescription}" adtcore:language="${masterLanguage}" adtcore:name="${params.structureName.toUpperCase()}" adtcore:type="TABL/DS" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
   <adtcore:packageRef adtcore:name="${params.packageName.toUpperCase()}"/>
 </blue:blueSource>`;
     const headers = {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/delete.js
index 6522aab..120c187 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/delete.js
@@ -72,6 +72,7 @@ async function deleteStructure(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Structure");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/types.d.ts
index 6d9eff6..1cb4792 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/structure/types.d.ts
@@ -23,6 +23,7 @@ export interface ICreateStructureParams {
     packageName: string;
     transportRequest?: string;
     masterSystem?: string;
+    masterLanguage?: string;
     responsible?: string;
 }
 export interface IUpdateStructureParams {
@@ -40,6 +41,7 @@ export interface IStructureConfig {
     transportRequest?: string;
     description?: string;
     ddlCode?: string;
+    masterLanguage?: string;
     fields?: IStructureField[];
     includes?: IStructureInclude[];
     onLock?: (lockHandle: string) => void;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/AdtTable.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/AdtTable.js
index f9ca732..63b3cc3 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/AdtTable.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/AdtTable.js
@@ -74,6 +74,8 @@ class AdtTable {
                 ddl_code: options?.sourceCode || config.ddlCode,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                // [powerup 4.13.12] Forward the resolved logon language (§ backlog 11-⑫).
+                masterLanguage: config.masterLanguage,
             });
             objectCreated = true;
             state.createResult = createResponse;
@@ -382,8 +384,23 @@ class AdtTable {
         }
         // Map status to version
         const version = status === 'active' ? 'active' : 'inactive';
+        // [powerup 4.13.12] Forward config.ddlCode as the source to validate.
+        // Callers that pass ddlCode (the UpdateTable high-level pre-check and
+        // the CheckTableLow tool's documented ddl_code path) intend to validate
+        // the NEW/unsaved DDL, not whatever version is already stored on the
+        // server. Dropping it silently (passing undefined) made the "check new
+        // code" step validate the wrong version — so a syntax error in the new
+        // DDL was never surfaced by the pre-check and the write PUT failed
+        // opaquely ("Kein Sichern wegen Fehler in Quelle. Details erhalten Sie
+        // mit Prüfung."). undefined (no ddlCode) preserves the previous
+        // saved-version behavior for callers that pass none. Same root as the
+        // 4.13.11 AdtStructure.check fix. runTableCheckRun still only POSTs and
+        // returns the raw response (it never throws on check errors) — the
+        // caller (UpdateTable handler / CheckTableLow) parses the result, so
+        // this stays non-throwing and does not change the CheckTableLow
+        // contract.
         return {
-            checkResult: await (0, check_1.runTableCheckRun)(this.connection, 'abapCheckRun', config.tableName, undefined, version),
+            checkResult: await (0, check_1.runTableCheckRun)(this.connection, 'abapCheckRun', config.tableName, config.ddlCode, version),
             errors: [],
         };
     }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/create.js
index f4ff8e2..b37411d 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/create.js
@@ -31,7 +31,13 @@ async function createTable(connection, params) {
         : '';
     // Create empty table with POST
     const createUrl = `/sap/bc/adt/ddic/tables${params.transport_request ? `?corrNr=${params.transport_request}` : ''}`;
-    const tableXml = `<?xml version="1.0" encoding="UTF-8"?><blue:blueSource xmlns:blue="http://www.sap.com/wbobj/blue" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="EN" adtcore:name="${params.table_name.toUpperCase()}" adtcore:type="TABL/DT" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr}>
+    // [powerup 4.13.12] Stamp the payload language with the system's logon /
+    // master language so the description lands in the right language slot. An
+    // EN-hardcoded payload stores the description in the EN text slot, which
+    // reads back empty on a non-EN logon system (HANDOFF §6 backlog 11-⑫).
+    // Callers resolve the logon language and pass it; EN stays as the fallback.
+    const masterLanguage = (params.masterLanguage || 'EN').toUpperCase();
+    const tableXml = `<?xml version="1.0" encoding="UTF-8"?><blue:blueSource xmlns:blue="http://www.sap.com/wbobj/blue" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${params.table_name.toUpperCase()}" adtcore:type="TABL/DT" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
 
   <adtcore:packageRef adtcore:name="${params.package_name.toUpperCase()}"/>
 
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/delete.js
index 36ebd1b..33de3fc 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/delete.js
@@ -71,6 +71,7 @@ async function deleteTable(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "Table");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/types.d.ts
index eb6aeb6..5e9b848 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/table/types.d.ts
@@ -8,6 +8,7 @@ export interface ICreateTableParams {
     transport_request?: string;
     ddl_code?: string;
     masterSystem?: string;
+    masterLanguage?: string;
     responsible?: string;
 }
 export interface IUpdateTableParams {
@@ -26,6 +27,7 @@ export interface ITableConfig {
     transportRequest?: string;
     ddlCode?: string;
     description?: string;
+    masterLanguage?: string;
 }
 export interface ITableState extends IAdtObjectState {
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/AdtView.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/AdtView.js
index 868d975..b5d5678 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/AdtView.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/AdtView.js
@@ -92,6 +92,7 @@ class AdtView {
                 ddl_source: options?.sourceCode || config.ddlSource,
                 masterSystem: this.systemContext.masterSystem,
                 responsible: this.systemContext.responsible,
+                master_language: config.masterLanguage,
             });
             objectCreated = true;
             state.createResult = createResponse;
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/create.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/create.js
index 86ac671..866034a 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/create.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/create.js
@@ -26,7 +26,15 @@ async function createDDLSObject(connection, args) {
     const responsibleAttr = responsible
         ? ` adtcore:responsible="${responsible}"`
         : '';
-    const metadataXml = `<?xml version="1.0" encoding="UTF-8"?><ddl:ddlSource xmlns:ddl="http://www.sap.com/adt/ddic/ddlsources" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="EN" adtcore:name="${args.view_name}" adtcore:type="DDLS/DF" adtcore:masterLanguage="EN"${masterSystemAttr}${responsibleAttr}>
+    // [powerup 4.13.10] The DDLS create service HARD-REJECTS a payload whose
+    // masterLanguage differs from the logon language ("Sprache EN zum Anlegen
+    // der Beschreibung entspricht nicht Mastersprache CS", T100
+    // DDIC_ADT_DDLS/016 → HTTP 400) — live-measured on an S/4HANA system
+    // logged on in CS (HANDOFF §6 backlog 11-⑧/11-④). Callers resolve the
+    // logon language from the live system and pass it as master_language;
+    // the historical EN default is kept as the fallback.
+    const masterLanguage = (args.master_language || 'EN').toUpperCase();
+    const metadataXml = `<?xml version="1.0" encoding="UTF-8"?><ddl:ddlSource xmlns:ddl="http://www.sap.com/adt/ddic/ddlsources" xmlns:adtcore="http://www.sap.com/adt/core" adtcore:description="${description}" adtcore:language="${masterLanguage}" adtcore:name="${args.view_name}" adtcore:type="DDLS/DF" adtcore:masterLanguage="${masterLanguage}"${masterSystemAttr}${responsibleAttr}>
   <adtcore:packageRef adtcore:name="${args.package_name}"/>
 </ddl:ddlSource>`;
     const headers = {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/delete.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/delete.js
index e8f0b89..945f84d 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/delete.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/delete.js
@@ -71,6 +71,7 @@ async function deleteView(connection, params) {
         data: xmlPayload,
         headers,
     });
+    (0, internalUtils_1.assertDeletionSucceeded)(response, "View");
     return {
         ...response,
         data: {
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/types.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/types.d.ts
index bcc0309..c29dd0f 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/types.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/core/view/types.d.ts
@@ -10,6 +10,7 @@ export interface ICreateViewParams {
     description?: string;
     masterSystem?: string;
     responsible?: string;
+    master_language?: string;
 }
 export interface IUpdateViewSourceParams {
     view_name: string;
@@ -28,6 +29,7 @@ export interface IViewConfig {
     transportRequest?: string;
     description?: string;
     ddlSource?: string;
+    masterLanguage?: string;
     sessionId?: string;
     onLock?: (lockHandle: string) => void;
 }
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/internalUtils.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/internalUtils.js
index 76c7154..9f388f8 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/internalUtils.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/internalUtils.js
@@ -9,6 +9,7 @@ exports.limitDescription = limitDescription;
 exports.headerValueToString = headerValueToString;
 exports.safeErrorMessage = safeErrorMessage;
 exports.safeStringify = safeStringify;
+exports.assertDeletionSucceeded = assertDeletionSucceeded;
 /**
  * Encodes SAP object names for use in URLs
  * Handles namespaces with forward slashes that need to be URL encoded
@@ -102,3 +103,76 @@ function safeStringify(value, maxLength = 500) {
         return String(value).substring(0, maxLength);
     }
 }
+/**
+ * [powerup 4.13.9] Honour the ADT deletion service's del:isDeleted flag.
+ *
+ * The generic `POST /sap/bc/adt/deletion/delete` service returns HTTP 200 even
+ * when it REFUSES the delete (e.g. the object is still locked): the real
+ * outcome is carried only by `del:deletionResult/del:object[@del:isDeleted]`
+ * plus an E-level `del:message`. The vendored low-level `deleteX()` helpers
+ * historically discarded that body and hardcoded `{ success: true }`, so a
+ * failed delete was reported to the caller as a success (HANDOFF §6 backlog
+ * 3-5, live-measured on DeleteFunctionGroup). This asserts the flag and throws
+ * with the SAP message on a POSITIVELY-identified failure. Unknown / absent
+ * deletionResult bodies fall back to the HTTP status only, so an unusual but
+ * successful response shape is never mis-reported as a failure. Mirrors the
+ * parse the vendored `deletePackage()` already performs.
+ *
+ * @param response - the raw axios response from the deletion POST
+ * @param objectLabel - human label for the error message (e.g. 'Function group')
+ */
+function assertDeletionSucceeded(response, objectLabel) {
+    const label = objectLabel || 'Object';
+    const data = response && response.data;
+    if (typeof data === 'string' && data.indexOf('deletionResult') !== -1) {
+        try {
+            const { XMLParser } = require('fast-xml-parser');
+            const parser = new XMLParser({
+                ignoreAttributes: false,
+                attributeNamePrefix: '@_',
+            });
+            const result = parser.parse(data);
+            let deleteObjects = result['del:deletionResult']?.['del:object'] ??
+                result.deletionResult?.object;
+            if (deleteObjects) {
+                // A single delete can cascade into several del:object entries
+                // (e.g. a structure returns both its TABL/DS and TABT/DTT
+                // nodes), and fast-xml-parser yields an ARRAY in that case.
+                // Every node must report isDeleted="true" for the delete to
+                // have succeeded; any node with isDeleted!="true" is a failure.
+                if (!Array.isArray(deleteObjects)) {
+                    deleteObjects = [deleteObjects];
+                }
+                for (const obj of deleteObjects) {
+                    const isDeleted = obj['@_del:isDeleted'] === 'true' ||
+                        obj['@_isDeleted'] === 'true';
+                    if (!isDeleted) {
+                        let text = obj['del:message']?.['del:text'] ??
+                            obj.message?.text;
+                        if (text && typeof text === 'object') {
+                            text = text['#text'] ?? '';
+                        }
+                        const msg = typeof text === 'string' && text.trim()
+                            ? text.trim()
+                            : 'the deletion service reported isDeleted="false"';
+                        throw new Error(`${label} deletion failed: ${msg}`);
+                    }
+                }
+            }
+        }
+        catch (error) {
+            if (error &&
+                typeof error.message === 'string' &&
+                error.message.includes('deletion failed')) {
+                throw error;
+            }
+            // Parse hiccup on a body that mentioned deletionResult but did not
+            // parse cleanly — fall through to the HTTP-status check below.
+        }
+    }
+    if (response &&
+        typeof response.status === 'number' &&
+        response.status >= 400) {
+        throw new Error(`${label} deletion failed: HTTP ${response.status} ${response.statusText || ''}`.trim());
+    }
+}
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.d.ts b/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.d.ts
index af031bf..7136af0 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.d.ts
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.d.ts
@@ -7,8 +7,13 @@
 /**
  * Replace an XML attribute value.
  * Example: `adtcore:description="old"` → `adtcore:description="new"`
+ *
+ * [powerup 4.13.10] `options.addIfMissing`: inject the attribute into the
+ * root element's opening tag when it is absent (default: no-op, unchanged).
  */
-export declare function patchXmlAttribute(xml: string, attrName: string, newValue: string): string;
+export declare function patchXmlAttribute(xml: string, attrName: string, newValue: string, options?: {
+    addIfMissing?: boolean;
+}): string;
 /**
  * Replace XML element text content.
  * Handles both `<tag>content</tag>` and self-closing `<tag/>`.
diff --git a/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.js b/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.js
index d758bd1..60b4d88 100644
--- a/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.js
+++ b/node_modules/@babamba2/mcp-abap-adt-clients/dist/utils/xmlPatch.js
@@ -15,10 +15,34 @@ exports.extractXmlString = extractXmlString;
 /**
  * Replace an XML attribute value.
  * Example: `adtcore:description="old"` → `adtcore:description="new"`
+ *
+ * [powerup 4.13.10] Optional `options.addIfMissing`: when the attribute is
+ * absent from the document, inject it into the ROOT element's opening tag
+ * instead of silently no-op'ing. SAP omits `adtcore:description` entirely
+ * from the GET XML of an object whose description never landed (e.g. a
+ * half-created skeleton left by an EN-payload create on a non-EN logon
+ * system — HANDOFF §6 backlog 11-⑧), and the replace-only behavior made such
+ * skeletons unrepairable via the read-modify-write Update path ("Die
+ * Beschreibung fehlt"). Default behavior (no options) is unchanged.
  */
-function patchXmlAttribute(xml, attrName, newValue) {
+function patchXmlAttribute(xml, attrName, newValue, options) {
     const regex = new RegExp(`${attrName}="[^"]*"`);
-    return xml.replace(regex, `${attrName}="${escapeXmlAttr(newValue)}"`);
+    if (regex.test(xml)) {
+        return xml.replace(regex, `${attrName}="${escapeXmlAttr(newValue)}"`);
+    }
+    if (options && options.addIfMissing) {
+        // Find the first element opening tag (skips the <?xml?> prolog and
+        // comments — those don't start with a name character). Quoted
+        // attribute values are consumed atomically so a '>' inside a value
+        // cannot end the match early.
+        const rootOpenRe = /<([A-Za-z][\w:.-]*)((?:"[^"]*"|'[^']*'|[^>"'])*?)(\/?)>/;
+        const m = xml.match(rootOpenRe);
+        if (m && m.index !== undefined) {
+            const newTag = `<${m[1]}${m[2]} ${attrName}="${escapeXmlAttr(newValue)}"${m[3]}>`;
+            return xml.slice(0, m.index) + newTag + xml.slice(m.index + m[0].length);
+        }
+    }
+    return xml;
 }
 /**
  * Replace XML element text content.
